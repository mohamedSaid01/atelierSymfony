<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails de l'Auteur</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Détails de l'Auteur</h1>
        
        <div class="card mb-4">
            <div class="card-body text-center">
                <img src="{{ asset(author.picture) }}" alt="{{ author.username }}" class="img-fluid" style="max-width: 200px; border-radius: 10px;"/>
                <h5 class="card-title mt-3">{{ author.username }}</h5>
            </div>
        </div>

        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th scope="row">Email</th>
                    <td>{{ author.email }}</td>
                </tr>
                <tr>
                    <th scope="row">Nombre de Livres</th>
                    <td>{{ author.nb_books }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_listeAuthor') }}" class="btn btn-secondary">Retour à la liste des auteurs</a>
    </div>

</body>
</html>
